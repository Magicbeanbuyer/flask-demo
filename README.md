# README

```shell
flask --app hello run
```

if the file is named app.py or wsgi.py, you don’t have to use `--app`.

## Flaskr

```shell
flask --app flaskr --debug run
```

see http://127.0.0.1:5000/hello.

